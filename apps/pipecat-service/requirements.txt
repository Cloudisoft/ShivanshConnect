# Phase 6: apps/pipecat-service - the self-hosted second call orchestration
# engine's own Python process. Run with:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   uvicorn app.main:app --host 0.0.0.0 --port 8100
#
# See README.md for the full run/deploy story (including Railway) and
# exactly which of the optional pipecat-ai provider extras below are
# actually exercised, gated on which env var is present.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0

# The real pipecat-ai framework (https://github.com/pipecat-ai/pipecat).
# Extras pull in the specific provider client libraries this service can
# use - each is only ever actually invoked when its matching env var
# (OPENAI_API_KEY / DEEPGRAM_API_KEY / ELEVENLABS_API_KEY / CARTESIA_API_KEY)
# is present; see app/pipeline.py and app/config.py. `twilio` brings in the
# Media Streams / TwiML helpers used to build the <Stream> response and to
# place the outbound call via Twilio's REST API.
pipecat-ai[openai,deepgram,elevenlabs,cartesia,twilio]==1.10.0

# Used to place outbound calls via Telnyx's REST API (Telnyx has no
# official Python SDK dependency pipecat-ai bundles - a plain HTTP call
# via httpx, already listed above, is used instead; kept here only as a
# documentation anchor for where Telnyx support lives in app/telephony.py).

# Test-only.
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
